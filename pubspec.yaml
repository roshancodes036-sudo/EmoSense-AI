name: emosense_ai
description: "A real-time sentiment analysis app powered by Gemini 1.5 Flash and Flutter."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

  dependencies:
    flutter:
        sdk: flutter

          # 1. The Brain (AI) 🧠
            # Google's official package for Gemini API
              google_generative_ai: ^0.4.0

                # 2. The Ears (Voice Input) 🎙️
                  # Converts speech to text in real-time
                    speech_to_text: ^6.6.1

                      # 3. The Visuals (Charts) 📊
                        # Beautiful charts to show Happy/Sad percentages (HR Requirement)
                          fl_chart: ^0.69.0

                            # 4. Permissions (Security) 🔐
                              # Handles Microphone permission smoothly on Android
                                permission_handler: ^11.3.1

                                  # 5. Formatting (Utils) 🕒
                                    # To format time in the Timeline list (e.g., "10:30 AM")
                                      intl: ^0.19.0

                                        # 6. UI Polish (Optional but Good) ✨
                                          # For nice icons (if needed)
                                            cupertino_icons: ^1.0.6

                                            dev_dependencies:
                                              flutter_test:
                                                  sdk: flutter
                                                    flutter_lints: ^3.0.0

                                                    # Assets section (For your images/logos)
                                                    flutter:
                                                      uses-material-design: true

                                                        # Agar aapne koi image folder banaya hai to ye line uncomment karein:
                                                          # assets:
                                                            #   - assets/images/